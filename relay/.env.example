PORT=8787
# On Fly.io, PORT is set in fly.toml [env] (8080) to match [http_service].internal_port.
CHAIN_ID=84532
# Leave empty for local dev so /v1/events/:eid/view accepts unsigned demo traffic.
INDEXER_SECRET=
